{% extends 'base.html.twig' %}

{% block title %}Create new Usine{% endblock %}

{% block body %}
    <h1>Create new Usine</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('usine_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
